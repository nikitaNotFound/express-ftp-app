import { RequestHandler } from "express";

export const createFolder: RequestHandler = (req, res) => {
  res.send(200);
};

export const deleteFolder: RequestHandler = (req, res) => {
  res.send(200);
};

export const getFolderContent: RequestHandler = (req, res) => {
  res.send(200);
};